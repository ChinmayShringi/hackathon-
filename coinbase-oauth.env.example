# Coinbase OAuth Configuration
# Copy this file to .env in your server directory and replace with your actual values

# OAuth Application Credentials
COINBASE_CLIENT_ID=your_client_id_here
COINBASE_CLIENT_SECRET=your_client_secret_here
COINBASE_REDIRECT_URI=https://yourdomain.com/auth/coinbase/callback

# Private Key Configuration (Choose ONE option)
# Option A: Path to private key file (recommended)
COINBASE_PRIVATE_KEY_PATH=secure/coinbase_private_key.pem

# Option B: Inline private key (escape newlines as \n)
# COINBASE_PRIVATE_KEY_PEM="-----BEGIN PRIVATE KEY-----\nMII...\n-----END PRIVATE KEY-----"

# OAuth Scopes (comma-separated)
COINBASE_SCOPES=wallet:accounts:read,wallet:transactions:read

# Token Expiration (in seconds)
COINBASE_JWT_EXPIRES_IN=300

# Environment
COINBASE_ENV=sandbox  # sandbox or production

# Instructions:
# 1. Copy this file to: MagicVidCreator/server/.env
# 2. Get your OAuth credentials from: https://www.coinbase.com/oauth/applications
# 3. Generate an Ed25519 key pair for JWT signing
# 4. Set up your redirect URI in the Coinbase OAuth application
# 5. Restart your development server

# Note: This implements the standard OAuth 2.0 flow for Coinbase
# The frontend will redirect users to Coinbase for authorization
# After authorization, Coinbase will redirect back with an authorization code
# Your backend will exchange the code for an access token using this endpoint
